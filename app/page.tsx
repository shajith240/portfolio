"use client";

// Home route. The desktop shell itself (wallpaper, ProfileWidget, MenuBar,
// Dock) is rendered globally in AppShell — this page is where additional
// home-screen widgets (GitHub/LeetCode/LinkedIn, etc.) will be added next.
// The previous draggable project-card carousel has been removed entirely:
// different direction, no more "canvas" panel.

export default function Home() {
  return null;
}
